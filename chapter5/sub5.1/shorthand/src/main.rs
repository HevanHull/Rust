fn main() {
    struct User{
        age: u32,
        email: String,
        username: String,
        address: String,
        active: bool,
        points: u64
    }
}

fn builduser (email: String, age: u32, username: String) -> User {
    User{
        email,
        age,
        usernmae
    }
}
